# Sherwood ops watchdog (auto-topup + health). Build context = repo root.
FROM node:22-slim
WORKDIR /app
RUN npm init -y >/dev/null 2>&1 && npm install --no-audit --no-fund viem@2.54.6
COPY deploy/vps/watchdog.mjs ./watchdog.mjs
ENV NODE_ENV=production
EXPOSE 8793
CMD ["node", "watchdog.mjs"]
